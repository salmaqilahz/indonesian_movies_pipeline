{
  "name": "indonesian-movies-pipeline",
  "image": "mcr.microsoft.com/devcontainers/python:3.11",

  "customizations": {
    "vscode": {
      "extensions": [
        "ms-python.python",
        "ms-python.vscode-pylance",
        "mtxr.sqltools",
        "mtxr.sqltools-driver-pg",
        "eamodio.gitlens"
      ]
    }
  },

  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y postgresql postgresql-contrib && sudo service postgresql start && pip install -r requirements.txt && cp -n .env.example .env",

  "postStartCommand": "sudo pg_ctlcluster 17 main start || true",

  "forwardPorts": [5432],

  "remoteUser": "vscode"
}
